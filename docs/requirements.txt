# Documentation build dependencies for Read the Docs.
# Keep in sync with the `docs` dependency group in pyproject.toml.
#
# stimela-ninja isn't published to PyPI yet, so `pip install .` (which
# Read the Docs uses, not uv) can't resolve dosho's own `stimela-ninja`
# dependency from an index -- install it from GitHub explicitly first.
# (Local dev uses `[tool.uv.sources]`'s editable sibling-checkout path
# instead; this pin is RTD/pip-specific.)
stimela-ninja @ git+https://github.com/SpheMakh/stimela-ninja.git
sphinx>=7.2
furo>=2024.1
sphinx-copybutton>=0.5
myst-parser>=2.0
